import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gt.gestorcontrasenias',
  appName: 'Gestor Contraseñas',
  webDir: 'www'
};

export default config;
